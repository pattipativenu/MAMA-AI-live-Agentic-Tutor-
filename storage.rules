rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write access ONLY if the user is signed in
      allow read, write: if request.auth != null;
    }
  }
}
